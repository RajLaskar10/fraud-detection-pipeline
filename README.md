# Fraud Detection Pipeline

End-to-end machine learning pipeline for detecting fraudulent credit card transactions. Built on the [Kaggle Credit Card Fraud Detection dataset](https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud) (284,807 transactions, 492 fraudulent — a 0.17% fraud rate).

Most fraud detection projects stop at model accuracy. This one goes further: it frames output around business decisions — the cost of missing real fraud vs. the cost of false alarms — and includes threshold tuning, a PostgreSQL storage layer, and a Power BI dashboard designed for compliance analyst review.

## Dataset

Download `creditcard.csv` from [Kaggle](https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud) and place it in `data/raw/`.

- 284,807 transactions over two days
- 492 fraudulent (0.17% of total)
- 28 PCA-anonymized features (V1-V28), plus Amount, Time, and Class

## Tech Stack

| Tool | Purpose |
|------|---------|
| Python 3.11 | Core language |
| Pandas | Data manipulation |
| NumPy | Numerical operations |
| scikit-learn | Model training and evaluation |
| PostgreSQL | Structured storage for scored transactions |
| Jupyter | Interactive exploration notebooks |
| Power BI | Dashboard for compliance review |
| matplotlib / seaborn | Plotting in notebooks |

## Project Structure

```
fraud-detection-pipeline/
├── data/
│   ├── raw/                              # Drop creditcard.csv here
│   └── processed/                        # Generated by pipeline scripts
├── notebooks/
│   ├── README.md                         # What each notebook covers
│   ├── 01_eda.ipynb                      # Exploratory data analysis
│   ├── 02_feature_engineering.ipynb      # Feature engineering walkthrough
│   └── 03_model_training.ipynb          # Model training and evaluation
├── pipeline/
│   ├── preprocessing/
│   │   └── clean.py                      # Step 1: scale Amount, save cleaned data
│   ├── features/
│   │   └── engineer.py                   # Step 2: create time/velocity/amount features
│   └── model/
│       ├── train.py                      # Step 3: train LR and RF classifiers
│       └── evaluate.py                   # Step 4: metrics, thresholds, comparison
├── db/
│   ├── schema.sql                        # PostgreSQL table and view definitions
│   └── load.py                           # Load scored results into PostgreSQL
├── powerbi/
│   └── setup.md                          # Power BI connection and dashboard setup
├── docs/
│   ├── feature-decisions.md              # Why each feature was engineered
│   ├── model-results.md                  # Placeholder for results after training
│   └── threshold-analysis.md             # How to pick the right threshold
├── requirements.txt                      # Python dependencies
├── .gitignore                            # Excludes data, models, env files
└── README.md                             # This file
```

## How to Run

### 1. Set up the environment

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 2. Get the data

Download `creditcard.csv` from [Kaggle](https://www.kaggle.com/datasets/mlg-ulb/creditcardfraud) and place it in `data/raw/`.

### 3. Run the pipeline

```bash
# Step 1: Clean and scale
python pipeline/preprocessing/clean.py

# Step 2: Engineer features
python pipeline/features/engineer.py

# Step 3: Train models
python pipeline/model/train.py

# Step 4: Evaluate and compare
python pipeline/model/evaluate.py
```

### 4. Pick a threshold

Review the threshold table printed by `evaluate.py`. See `docs/threshold-analysis.md` for guidance. Update `FLAG_THRESHOLD` in `db/load.py` with your chosen value.

### 5. Load into PostgreSQL

```bash
# Create the database and tables
psql -U postgres -c "CREATE DATABASE fraud;"
psql -U postgres -d fraud -f db/schema.sql

# Load scored transactions
python db/load.py
```

Set connection details via environment variables (`DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`) or use the defaults (localhost, 5432, fraud, postgres, postgres).

### 6. Connect Power BI

Follow the instructions in `powerbi/setup.md` to build the dashboard.

## Key Design Decisions

- **Feature engineering choices**: See [docs/feature-decisions.md](docs/feature-decisions.md) — covers why each feature was added, what was dropped, and known limitations
- **Threshold tuning**: See [docs/threshold-analysis.md](docs/threshold-analysis.md) — explains why 0.5 is wrong for imbalanced data and how to pick a better threshold
- **Model results**: See [docs/model-results.md](docs/model-results.md) — fill in after running the pipeline


