# Raw and processed data (keep .gitkeep so dirs exist in repo)
data/raw/*
!data/raw/.gitkeep
data/processed/*
!data/processed/.gitkeep

# Model files (exclude large intermediates but allow the trained models)
*.pkl
!data/processed/lr_model.pkl
!data/processed/rf_model.pkl

# Environment
.env

# Python
__pycache__/
*.pyc

# Jupyter
.ipynb_checkpoints/

# Project spec (not part of deliverable)
antigravity-fraud-detection.md

# Claude Code internal files
CLAUDE.md
.mcp.json
